experiment:
  run_prefix: "elicitation_repro"
  output_root: "results/repro"

base:
  steps: 90
  checkpoints: 1
  mcem:
    n_iterations: 8
    n_gibbs_samples: 30
    n_gibbs_burnin: 15

sweep:
  seed: {start: 0, stop: 29, step: 1}
  dataset: ["ds1", "ds2", "sushi"]
  criterion: ["social", "personalized", "random", "d_opt", "e_opt"]

aggregations: ["criteria_curve", "timing"]
