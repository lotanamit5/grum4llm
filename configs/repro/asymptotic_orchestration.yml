experiment:
  run_prefix: "asymptotic_repro"
  output_root: "results/repro"

base:
  steps: 500
  checkpoints: 50
  mcem:
    n_iterations: 8
    n_gibbs_samples: 30
    n_gibbs_burnin: 15

sweep:
  seed: {start: 0, stop: 9, step: 1}
  dataset: ["ds0"]
  criterion: ["social", "personalized"]

aggregations: ["criteria_curve", "timing"]
